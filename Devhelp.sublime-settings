{
    // Devhelp executable path. Subject to $PATH resolution.
    "devhelp_command": "devhelp",

    // List of selectors (https://www.sublimetext.com/docs/3/selectors.html)
    // for which devhelp commands will be enabled.
    // Empty list will enable commands always.
    "devhelp_selectors": [
        "source.c",
        "source.c++"
    ]
}
